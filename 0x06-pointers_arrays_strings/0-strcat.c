#include "main.h"

/**
 * *_strcat - concatenates two strings
 * @dest: char pointer
 * @src: char pointer
 * Return: 0
 */
char *_strcat(char *dest, char *src)
{
	char *Value = strcat(dest, src);

	return (Value);
}
