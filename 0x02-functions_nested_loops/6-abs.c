#include "main.h"

/**
 * _abs - lowercase character
 * @n: The _abs to checked
 * Return: 1 if n greater than zero
 */
int _abs(int n)
{
	if (n > -1)
		_putchar(n);
	else if (n == 0)
		_putchar(n);
	else if (n == -98)
		_putchar(n);
	else
		_putchar(n);
}
